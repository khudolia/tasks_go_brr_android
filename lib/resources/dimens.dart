import 'package:flutter/material.dart' as mtrl;

class Dimens {
  static const bottom_app_bar_height = 64.0;
}

class Durations {
  static const milliseconds_short = Duration(milliseconds: 200);
}

class Radius {
  static const middle = mtrl.Radius.circular(30.0);
}